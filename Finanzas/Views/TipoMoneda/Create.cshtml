@model Finanzas.Models.TipoMoneda

@{
    ViewBag.Title = "Nuevo";
}
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="panel-btns">
            <a href="#" class="panel-close">&times;</a>
            <a href="#" class="minimize">&minus;</a>
        </div>
        <!-- panel-btns -->
        <h3 class="panel-title">Nuevo</h3>
    </div>
    <div class="panel-body">
        <div class="col-sm-4">
            @using (Html.BeginForm())
            {
                @Html.ValidationSummary(true)
                <fieldset>
                    <legend>TipoMoneda</legend>

                    <div class="form-group">
                        @Html.LabelFor(model => model.siglas)
                    </div>
                    <div class="form-group">
                        @Html.TextBoxFor(model => model.siglas, new { Class = "form-control" })
                        @Html.ValidationMessageFor(model => model.siglas)
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.moneda)
                    </div>
                    <div class="form-group">
                        @Html.TextBoxFor(model => model.moneda, new { Class = "form-control" })
                        @Html.ValidationMessageFor(model => model.moneda)
                    </div>

                    <div class="btn-group">
                        @Html.ActionLink("Atras", "Index", null, new { Class = "btn btn-info" })
                        <input class="btn btn-success" type="submit" value="Nuevo" />
                    </div>
                </fieldset>
        }
        </div>
    </div>
    <!-- panel-body -->
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
